import 'package:flutter/material.dart';

class Ping extends StatelessWidget {
  const Ping({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("قيد الانجاز"));
  }
}
